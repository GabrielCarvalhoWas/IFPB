# Exercicio 004
# Escreva um programa para calcular a área de um triângulo, dados os valores de
# base e altura. Área = base x altura / 2
base = float(input("Digite o valor da base: "))
altura = float(input("Digite o valor da altura: "))
area = (base * altura) / 2
print(f"Área: {area}")